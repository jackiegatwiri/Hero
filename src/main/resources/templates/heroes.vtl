<h1>Hero list!</h1>
#if ($heroes)
<ul>
    #foreach ($hero in $heroes)
    <li><a href="/heroes/$hero.getId()">$hero.getName()
        $hero.getAge()
        $hero.getPower()
        $hero.getWeakness()</a></li>
    #end
</ul>
#else
<p>No heroes!</p>
#end

<a href="/heroes/new">Add a new hero</a>